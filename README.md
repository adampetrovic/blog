# Blog

Minimal Hugo blog using the [`hugo-bearblog`](https://github.com/janraasch/hugo-bearblog) theme.

## Local development

```bash
hugo server -D
```

## Publishing

GitHub Actions builds the site and publishes it to GitHub Pages on every push to `main`.
